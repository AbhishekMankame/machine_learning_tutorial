# __init__.py is a special file used in Python to define packages and intialize their namespaces

# Note: If we need to initialize the entire folder and define packages or modules then we have to use __init__.py, even though it is empty keep it empty